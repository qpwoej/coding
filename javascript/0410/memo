return O x 함수 실행시 필요한 데이터 O x
return 일을 시킨 사람에게 결과를 전달
결과가 이미 나왔기때문에 더이상 함수진행이 불필요함


객체(object)
일상생활에 있는대상을 코드로표현
변수와 값,함수를 가지고있음
객체를 이용을 하면 이미 만들어진 데이터와 함수를 사용할 수 있음

객체사용방법
1.객체 생성(객체에 따라 이미 생성되있는 객체도 있음)
2.객체.변수 = 데이터,객체.함수()  .을 이용해서 객체가 가지고있는 함수 및 변수,값을 사용
   .연산자는 객체 안에 있는...
document.write(); -->document 객체 안에 있는 write 함수를 사용하겠다

내장객체 --자바스크립트에서 지원하는 기능 

날짜,수학,윈도우,사용자정보,문자열,배열,사용자정보

